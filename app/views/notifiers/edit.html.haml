%p#breadcrumb
  = link_to 'Applications', root_path
  »
  = link_to @application.name, @application
  »
  = link_to 'Notifiers', application_path(@application, :anchor => 'notifiers')
  » Edit notifier

%h1 Editing notifier

= render 'form'
